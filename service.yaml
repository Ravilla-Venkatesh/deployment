apiVersion: v1
kind: Service
metadata:
  name: my-service
  namespace: venky
spec:
  selector:
    app: dotnetdemo
    type: Loadbalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
